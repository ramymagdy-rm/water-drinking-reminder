// m5stack-water-reminder — WiFi credentials template
//
// Copy this file to src/secrets.hpp (gitignored) and fill in your network.
// Leaving WIFI_SSID empty disables NTP sync entirely — the device falls
// back to the manual set-hour / set-minute menu items.

#pragma once

#define WIFI_SSID ""
#define WIFI_PASS ""
